# Agent GuidelinesInstructions for AI coding agents (Cursor and similar tools) working in the **Health AI Platform Pro** repository.## Project ContextThis is a **commercial AI-powered healthcare platform**. All active backend development happens under `backend/` using FastAPI and Clean Architecture. The legacy Flask demo in `health-ai-platform-clean-master/` is **deprecated** and must not be modified, extended, or used as a reference for patterns.Frontend for the pilot lives under `backend/frontend/` (Next.js).---## Health AI Platform Pro â€” Agent Guardrails### Genel Ã§alÄ±ÅŸma- Ã–nce mevcut kodu, testleri ve mimariyi incele; tahmin ederek Ã¶zellik ekleme.- Pilot MVP kapsamÄ±nÄ± koru; **bitir-kapat** yaklaÅŸÄ±mÄ± kullan.- Gereksiz refactor yapma; **en kÃ¼Ã§Ã¼k gÃ¼venli diff**'i tercih et.- Yeni dependency eklemeden Ã¶nce gerekÃ§e belirt (backend: `requirements.txt` + `pyproject.toml`).- KullanÄ±cÄ± aÃ§Ä±kÃ§a istemeden **commit/push yapma**.- Mevcut Ã§alÄ±ÅŸan davranÄ±ÅŸÄ± bozmadan ilerle; kapsamÄ± kullanÄ±cÄ± istemeden bÃ¼yÃ¼tme.### GÃ¼venlik- Auth, RBAC, PatientAccessPolicy, organization isolation ve patient isolation kurallarÄ±nÄ± **gevÅŸetme**.- Yetkisiz hasta eriÅŸiminde **404 masking** davranÄ±ÅŸÄ±nÄ± koru (veri sÄ±zdÄ±rma yok).- Public self-registration Ã¼zerinden `clinic_admin` veya `system_admin` oluÅŸturulmasÄ±na izin verme.- Secret, token, password, `DATABASE_URL` veya credential **loglama/commit etme**.- `.env.local` ve gerÃ§ek production secret dosyalarÄ±nÄ± commit etme.- Development kolaylÄ±ÄŸÄ± iÃ§in production **CORS**'u gevÅŸetme.- Hassas hata ayrÄ±ntÄ±larÄ±nÄ± kullanÄ±cÄ±ya sÄ±zdÄ±rma.### SaÄŸlÄ±k Ã¼rÃ¼nÃ¼- Sistem **tanÄ± koymaz**; klinik **karar destek** Ã¼rÃ¼nÃ¼dÃ¼r.- Klinik olarak doÄŸrulanmamÄ±ÅŸ risk modellerini â€œtanÄ±â€, â€œkesin sonuÃ§â€ veya â€œklinik olarak doÄŸrulanmÄ±ÅŸâ€ ÅŸeklinde sunma.- Risk ekranlarÄ±nda ve PDF'de karar destek / sorumluluk reddi metnini koru.- GerÃ§ek hasta verisi yerine geliÅŸtirme ve testte **sentetik veri** kullan.- e-NabÄ±z, LIS, PACS, MR/BT, RAG/LLM gibi ileri faz Ã¶zelliklerini pilot gerektirmiyorsa ekleme.### Pilot Ã¶nceliÄŸi1. Doctor workflow2. Clinic admin workflow3. Patient access security4. Clinical Timeline5. Risk history6. PDF / reporting7. Consent / KVKK8. Audit9. Production stability10. E2E / live verification### RAG korumasÄ±AÅŸaÄŸÄ±daki dosyalarda **mevcut uncommitted RAG Ã§alÄ±ÅŸmasÄ±** varsa kullanÄ±cÄ± onayÄ± olmadan **dokunma, silme, stage veya commit etme** (paths `backend/` altÄ±ndadÄ±r):- `backend/app/main.py`- `backend/app/infrastructure/embeddings/embedding_factory.py`- `backend/tests/api/test_rag_disabled_pilot.py`- Pilot production'da **`RAG_ENABLED=false`** varsay.- Fake embedding kullanma.- Production gÃ¼venlik kontrollerini kaldÄ±rma.### Test baÄŸÄ±msÄ±zlÄ±ÄŸÄ±- Implementation mantÄ±ÄŸÄ±nÄ± testte **birebir kopyalama**.- Testleri **acceptance criteria** ve **gÃ¶zlenebilir davranÄ±ÅŸ** Ã¼zerinden yaz.- Happy path dÄ±ÅŸÄ±nda edge-case testleri Ã¼ret, Ã¶rneÄŸin:  - invalid input, boundary cases  - unauthorized access, cross-organization access  - unassigned patient, inactive assignment / membership  - expired / revoked token, duplicate operation  - missing data, network/backend failure, malformed response- **â€œAll tests passâ€** tek baÅŸÄ±na yeterli deÄŸildir; her kritik gÃ¶revde **blind spots** raporla.- MÃ¼mkÃ¼n olduÄŸunda **unit + API/integration + live verification** kullan.### Git- Ã–nce `git status`.- YalnÄ±z gÃ¶rev kapsamÄ±ndaki dosyalarÄ± stage et; unrelated deÄŸiÅŸiklikleri commit etme.- `git diff --check` Ã§alÄ±ÅŸtÄ±r.- Secret scan yap (staged diff / commit adayÄ±).- Testler fail ise **commit/push yapma**.- Commit mesajÄ± yapÄ±lan iÅŸi aÃ§Ä±kÃ§a tanÄ±mlasÄ±n.### ProductionDeploy sonrasÄ± doÄŸrula:- `/api/v1/health`- `/api/v1/ready`Environment deÄŸiÅŸikliklerinin yeni process'e yansÄ±dÄ±ÄŸÄ±nÄ± kontrol et. Local testleri production **PASS** kanÄ±tÄ± kabul etme; kritik akÄ±ÅŸlarÄ± canlÄ± veya production-benzeri ortamda doÄŸrula.### i18n- TÃ¼rkÃ§e seÃ§iliyken kullanÄ±cÄ±ya gÃ¶rÃ¼nen iÃ§erik **tamamen TÃ¼rkÃ§e**.- English seÃ§iliyken **tamamen Ä°ngilizce**.- `seed` / test / internal marker kullanÄ±cÄ± arayÃ¼zÃ¼ne veya PDF'ye **sÄ±zmamalÄ±**.- Locale hem otomatik test hem **manuel Ã§Ä±ktÄ±** ile doÄŸrulanmalÄ± (Ã¶r. PDF `X-Report-Locale`, proxy vs. canlÄ± API sÃ¼rÃ¼mÃ¼).### GÃ¶rev sonu raporuHer gÃ¶rev sonunda (kullanÄ±cÄ± farklÄ± format istemediyse) ÅŸunlarÄ± raporla:- Changed files- Root cause- Exact behavior changed- Tests run- Test results- Security impact- Remaining blind spots- Manual verification needed- Ready to commit: YES/NO---## Hard Rules1. **Do not touch legacy code** â€” never modify files under `health-ai-platform-clean-master/`.2. **Work in `backend/`** â€” all new Python API code goes under `backend/app/`.3. **Follow Clean Architecture** â€” respect layer boundaries (see [ARCHITECTURE.md](ARCHITECTURE.md)).4. **Do not skip tests** â€” every new module needs endpoint tests and an in-memory repository for the test suite.5. **Do not commit secrets** â€” never write real credentials into `.env`, source code, or documentation.6. **Minimize scope** â€” implement only what is requested; do not refactor unrelated modules.7. **Match existing conventions** â€” read surrounding code before adding new files; reuse patterns from Auth and Patient modules.8. **Do not break Authentication** â€” the Auth module is verified and production-ready; changes must be intentional and tested.## Cursor-Specific Guidance### Before Writing Code1. **Read first** â€” use `@` file references or the file explorer to inspect existing modules before creating new ones. Start with:   - `backend/app/api/deps.py` (dependency injection patterns)   - `backend/app/application/services/patient_service.py` (service pattern)   - `backend/app/infrastructure/repositories/patient_repository.py` (repository pattern)   - `backend/tests/conftest.py` (test fixture overrides)2. **Use Plan mode for large features** â€” switch to Plan mode when a task touches multiple layers or has architectural trade-offs (e.g., new entity relationships, authorization models).3. **Scope agent edits narrowly** â€” when implementing a module, specify which layers to create, which modules must not be modified, and expected endpoints and test cases.4. **Verify after every change** â€” run from `backend/`:   ```powershell   .\.venv\Scripts\Activate.ps1   pytest tests/ -v   ```5. **Use Cursor rules** â€” follow `.cursor/rules/` when present. Do not create new rules unless explicitly asked.6. **Branch discipline** â€” use descriptive branch names (e.g., `feature/disease-records`). Do not commit directly to main without review unless the user explicitly requests it.7. **Avoid editing generated or environment files** â€” do not modify `.venv/`, `__pycache__/`, or `.pytest_cache/`.### What Cursor Should Not Do- Rewrite `backend/app/main.py` or `backend/app/api/deps.py` without a clear reason (and respect **RAG korumasÄ±** above).- Replace in-memory test repositories with live database calls in tests.- Add frontend code outside `backend/frontend/` unless explicitly requested.- Run destructive database commands (`DROP DATABASE`, `alembic downgrade base`) without explicit approval.- Install new dependencies without updating both `requirements.txt` and `pyproject.toml`.## Where to Work| Task | Location ||---|---|| New entity | `backend/app/domain/entities/` || Repository port | `backend/app/domain/interfaces/` || ORM model | `backend/app/infrastructure/database/models/` || Repository impl | `backend/app/infrastructure/repositories/` || Business logic | `backend/app/application/services/` || Internal DTOs | `backend/app/application/dtos/` || API schemas | `backend/app/api/schemas/` || Endpoints | `backend/app/api/v1/endpoints/` || Router registration | `backend/app/api/v1/router.py` || DI providers | `backend/app/api/deps.py` || Migrations | `backend/alembic/versions/` || Tests | `backend/tests/api/` || Test fakes | `backend/tests/support/` || Pilot frontend | `backend/frontend/src/` |## Patterns to Follow### Repository Port```pythonclass ExampleRepository(Repository[Example]):    @abstractmethod    async def get_by_id_and_owner(self, entity_id: UUID, owner_id: UUID) -> Example | None: ...```### Service Error HandlingRaise typed exceptions from `app/core/exceptions.py`:| Exception | HTTP Status ||---|---|| `NotFoundError` | 404 || `UnauthorizedError` | 401 || `ForbiddenError` | 403 || `ConflictError` | 409 || `ValidationError` | 422 |Global handlers in `app/middleware/exception_handlers.py` convert these to JSON responses.### Endpoint AuthAll business routes must use `CurrentUser`:```pythonasync def create_item(    body: ItemCreate,    current_user: CurrentUser,    service: Annotated[ItemService, Depends(get_item_service)],) -> ItemResponse: ...```### Router RegistrationAdd new routers in `app/api/v1/router.py`:```pythonapi_v1_router.include_router(items.router, prefix="/items", tags=["Items"])```### Alembic MigrationAfter adding ORM models:1. Import model in `app/infrastructure/database/models/__init__.py`2. Create revision: `alembic revision -m "create items table"`3. Verify offline SQL: `alembic upgrade head --sql`4. Apply: `alembic upgrade head`Runtime uses **asyncpg**; Alembic uses **psycopg** via the `database_url_sync` setting property.## Testing Conventions| Convention | Detail ||---|---|| Framework | pytest + pytest-asyncio || Location | `backend/tests/api/test_<module>.py` || In-memory repos | `backend/tests/support/memory_<module>_repository.py` || DI override | Register in `backend/tests/conftest.py` via `app.dependency_overrides` || Auth helper | Register + login via `/api/v1/auth/register` and `/login` in test fixtures || Security tests | Always test 401 (unauthenticated) and 404 (cross-user / cross-org as applicable) |Tests must pass without a running PostgreSQL instance for the default API suite. See **Test baÄŸÄ±msÄ±zlÄ±ÄŸÄ±** above for acceptance-style and edge-case expectations.## Module Completion ChecklistBefore marking a module as done, verify:- [ ] Domain entity and repository port created- [ ] ORM model registered in `models/__init__.py`- [ ] SQLAlchemy repository implements all port methods- [ ] Application service with typed exceptions- [ ] Pydantic API schemas (Create, Update, Response, ListResponse)- [ ] Endpoints registered in v1 router- [ ] `get_<module>_service` added to `deps.py`- [ ] Alembic migration created and SQL verified- [ ] In-memory repository for tests- [ ] Endpoint tests: create, list, get, update, delete, 401, cross-user 404- [ ] Full suite passes: `pytest tests/ -v`- [ ] No unrelated files modified## Useful CommandsRun backend commands from `backend/` with the virtual environment activated.```powershell# Activate venv.\.venv\Scripts\Activate.ps1# Install dependenciespip install -r requirements-dev.txt# Run all tests (exclude embedding runtime if needed)pytest tests/ -vpytest tests -m "not embedding_runtime" -q# Run focused testspytest tests/api/test_patients.py -v# Start API serveruvicorn app.main:app --reload --host 127.0.0.1 --port 8001# Alembicalembic historyalembic upgrade headalembic upgrade head --sql# Lint (optional)ruff check app tests alembic```Frontend (from `backend/frontend/`):```powershellnpm run testnpm run typecheck```## Reference ModulesWhen implementing a new feature, treat these as canonical examples:| Module | Reference Files ||---|---|| Authentication | `app/application/services/auth_service.py`, `app/api/v1/endpoints/auth.py`, `tests/api/test_auth.py` || Patient CRUD | `app/application/services/patient_service.py`, `app/api/v1/endpoints/patients.py`, `tests/api/test_patients.py` |Copy the structure, not the code verbatim â€” adapt field names, relationships, and business rules to the new domain.
